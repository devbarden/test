// ═══════════════════════════════════════════════════════════════════════════
//   Media queries cannot read custom properties: these repeat the
//   `(width < …)` breakpoints used in the CSS modules.
// ═══════════════════════════════════════════════════════════════════════════
export const PHONE_QUERY = '(width < 40rem)'

export const STACKED_LAYOUT_QUERY = '(width < 60rem)'
