node_modules
.output
.nitro
.tanstack
.vite
dist
coverage
.env
.env.*
!.env.example
.DS_Store
*.log

# ═══════════════════════════════════════════════════════════════════════════
#   Clerk's keyless mode writes a temporary development instance here when
#   no keys are configured. It belongs to one machine and is never shared.
# ═══════════════════════════════════════════════════════════════════════════
.clerk

# ═══════════════════════════════════════════════════════════════════════════
#   Playwright traces, screenshots and reports from local runs.
# ═══════════════════════════════════════════════════════════════════════════
test-results
playwright-report

# ═══════════════════════════════════════════════════════════════════════════
#   The Prisma client is generated on install and on build from
#   prisma/schema.prisma; committing it would only let the two drift.
# ═══════════════════════════════════════════════════════════════════════════
src/generated
